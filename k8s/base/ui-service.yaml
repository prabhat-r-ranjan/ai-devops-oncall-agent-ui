apiVersion: v1
kind: Service
metadata:
  name: ai-devops-oncall-agent-ui
  labels:
    app: ai-devops-oncall-agent-ui
    component: ui
spec:
  selector:
    app: ai-devops-oncall-agent-ui
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
